# 프로젝트 버저닝 가이드 (Versioning & Deployment Guide)

## 📌 배포 시 필수 수칙
본 어플리케이션(Fire-Sight)의 코드를 수정하고 **`npm run deploy` 명령어를 통해 라이브 배포(Hosting)를 진행할 때는 반드시 버전을 한 단계 이상 올려야 합니다.**

### 🛠️ 버전 갱신이 필수적인 파일 (2곳)
기능 추가 혹은 버그 패치 후 배포 시, 다음 두 개의 파일을 **함께 동기화하여 모두 수정**해야 합니다.

1. **`package.json`**
   - 수정 항목: 최상단의 `"version"` 필드
   - 예시: `"version": "3.2.2"` -> `"version": "3.2.3"`
   - 목적: NPM 에코시스템 및 버전 관리 도구와의 규격 통일 

2. **`src/App.jsx`**
   - 수정 항목: 최상단 전역 변수 `APP_VERSION` 필드
   - 예시: `const APP_VERSION = 'v3.2.2';` -> `const APP_VERSION = 'v3.2.3';`
   - 목적: 프론트엔드의 **강제 캐시 플러시(Cache Invalidation)**. 이 값이 변해야 사용자의 모바일 브라우저에 남아있던 PWA 캐시 찌꺼기가 밀려나고 새로운 업데이트 코드가 즉시 덮어씌워집니다.

## ⚠️ 주의 사항
UI/UX 디자인 변경이나 중대한 백엔드 훅(`useFirestoreSync` 등) 리팩토링 직후에 `APP_VERSION` 업데이트를 누락하면, 배포가 성공하더라도 정작 접속자는 이전 버전의 하얀 화면(캐시 체류)만 보게 될 수 있습니다. **배포 직전 버전을 확인하는 것을 습관화합시다.** 🚀
