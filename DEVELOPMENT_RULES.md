# Fire-Sight Pro 개발 및 배포 통합 가이드라인 (Development Rules)

본 문서는 안티그래비티(AI)와 개발자(USER)가 협업하여 코드를 수정하고 배포할 때 반드시 지켜야 할 **핵심 수칙**을 정의합니다.

---

## 🛑 Rule 1. 코드 수정 전 '사전 컨셉 승인' 필수 (Planning Mode)
안티그래비티는 사용자의 요청에 따라 즉시 코드를 수정하거나 배포하기 전에, **반드시 변경하려는 컨셉(기획/설계 방향)을 먼저 제시하고 사용자의 확인(승인)을 받아야 합니다.**

1. **컨셉 브리핑:** 수정할 파일, 변경될 로직, 예상되는 UI 변화 등을 요약하여 설명합니다.
2. **부작용 검토:** 해당 변경으로 인해 기존에 잘 되던 기능(예: 기존 필터, 기존 채점 로직 등)이 망가질 가능성이 없는지 사전 검토 내역을 공유합니다.
3. **승인 대기:** 사용자가 "진행해" 또는 "OK" 등 명시적인 승인을 내린 직후에만 실제 코드 수정 및 Git 업데이트를 시작합니다.

---

## 📌 Rule 2. 라이브 배포 시 '버전 업넘버링' 필수 (Versioning)
본 어플리케이션의 코드를 수정하고 `npm run deploy` 명령어를 통해 라이브 배포(Hosting)를 진행할 때는 **반드시 버전을 한 단계 이상 올려야 합니다.**

### 🛠️ 버전 갱신이 필수적인 파일 (2곳 동기화)
기능 추가 혹은 버그 패치 후 배포 시, 다음 두 개의 파일을 반드시 동일한 버전으로 갱신해야 합니다.

1. **`package.json`**
   - 수정 항목: 최상단의 `"version"` 필드 (예: `"version": "3.2.8"`)
   - 목적: NPM 에코시스템 및 버전 관리 도구와의 규격 통일 

2. **`src/App.jsx`**
   - 수정 항목: 최상단 전역 변수 `APP_VERSION` 필드 (예: `const APP_VERSION = 'v3.2.8';`)
   - 목적: 프론트엔드의 **강제 캐시 플러시(Cache Invalidation)**. 이 값이 변해야 사용자의 브라우저(PWA)에 남아있던 과거 캐시 찌꺼기가 밀려나고 새로운 코드가 즉시 덮어씌워집니다.

> **⚠️ 주의 사항:** `APP_VERSION` 업데이트를 누락하면, 배포가 성공하더라도 정작 접속자는 이전 버전의 화면(또는 버그가 있던 상태)만 보게 될 수 있습니다. 배포 직전 버전을 확인하는 것을 습관화합시다.
